# AutoPartsWarehouse (Система управления складом автозапчастей)

[![Build Projects](https://github.com/Evklidiy/lab3/actions/workflows/main.yml/badge.svg)](https://github.com/Evklidiy/lab3/actions/workflows/main.yml)

Лабораторная работа №3 по дисциплине "Разработка приложений баз данных".  
**Тема:** Обработка HTTP запросов средствами ASP.NET Core. Сохранение состояния. Кэширование.

## 📋 Описание
Проект представляет собой веб-приложение на **ASP.NET Core 8.0** (без использования MVC), реализующее управление складом автозапчастей. Приложение демонстрирует работу с Middleware, маршрутизацией, кэшированием данных и сохранением состояния пользователя.

**Вариант задания:**
*   **Предметная область (Вариант 41):** Склад автозапчастей (7 таблиц).
*   **Расчетный вариант (Вариант 13):** Время кэширования `2 * 13 + 240 = 266` секунд.

## 🚀 Функционал

1.  **Кэширование (IMemoryCache):**
    *   Реализован сервис `CachedService`, кэширующий данные из БД на **266 секунд**.
    *   Данные автоматически загружаются в кэш (20 записей на таблицу) при старте.

2.  **Middleware и Маршрутизация:**
    *   `app.MapGet("/")` — Главная страница (Меню).
    *   `/info` — Информация о клиенте (IP, Браузер, Протокол).
    *   `/spareparts`, `/suppliers`, `/sales` и др. — Вывод таблиц с данными из кэша.
    *   **Обработка ошибок:** Если адрес не найден, возвращается кастомная страница **404 Page Not Found**.

3.  **Сохранение состояния:**
    *   `/searchform1` — Форма поиска с сохранением параметров в **Cookies** (время жизни 5 минут).
    *   `/searchform2` — Форма поиска с сохранением параметров в **Session**.

4.  **База данных:**
    *   Используется **MS SQL Server** (LocalDB) и **Entity Framework Core**.
    *   Реализован инициализатор БД (`DbInitializer`), который автоматически создает базу и наполняет её тестовыми данными (по 20 записей), если она пуста.

## 🛠 Технологии
*   .NET 8.0 (ASP.NET Core)
*   Entity Framework Core
*   Microsoft SQL Server
*   HTML/CSS (Inline styles)
*   GitHub Actions (CI/CD)

## ⚙️ Как запустить

1.  Клонируйте репозиторий:
    ```bash
    git clone https://github.com/ВАШ_ЛОГИН/ИМЯ_РЕПОЗИТОРИЯ.git
    ```
2.  Откройте проект в Visual Studio 2022.
3.  Проверьте строку подключения в `appsettings.json` (по умолчанию настроена на LocalDB):
    ```json
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=AutoPartsDB41;Trusted_Connection=True;MultipleActiveResultSets=true"
    ```
4.  Запустите проект (F5). База данных создастся автоматически.

## 👤 Автор
Студент группы [Ваша Группа]  
[Ваша Фамилия Имя]
